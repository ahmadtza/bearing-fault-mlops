apiVersion: 1

providers:
  - name: "Bearing Fault Dashboards"
    orgId: 1
    folder: "Bearing Fault MLOps"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: false

    options:
      path: /var/lib/grafana/dashboards
